# <项目名> 依赖分析报告

> 分析时间：<日期>
> 项目路径：`<路径>`
> 主仓库远端：`<git_remote_url>`
> 构建系统：<Bazel/CMake/混合>

---

## 1. 主仓库信息

| 字段 | 值 |
|------|----|
| 远端仓库 | `<url>` |
| 当前分支 | `<branch>` |
| Git 子模块 | 无 / [列出子模块名和 URL] |

---

## 2. Git 子模块依赖

| 子模块名 | 远端 URL | 当前 Commit | ARM 分支 | ARM 提交历史 | 兼容性状态 |
|----------|----------|------------|---------|------------|-----------|
| `fmt` | `https://github.com/...` | `abc1234` | — | — | ✅ 开源，无需检查 |
| `internal-sdk` | `ssh://git@git.example.internal/...` | `def5678` | 无 | 无 | 🔴 未知，需确认 |

---

## 3. 远端 Git 仓库依赖（Bazel/CMake）

| 名称 | 远端仓库 | 版本/Commit | 引入方式 | ARM 分支 | ARM 提交历史 | 兼容性状态 |
|------|----------|------------|---------|---------|------------|-----------|

---

## 4. HTTP/压缩包下载依赖

### 4.1 含源码（可本地编译）

| 名称 | 下载地址 | 版本 | 校验值 | 说明 |
|------|----------|------|--------|------|

### 4.2 ⚠️ 预编译二进制包（无源码，无法重新编译）

| 名称 | 下载地址 | 版本 | 校验值 | 内容说明 | 引入方式 |
|------|----------|------|--------|----------|---------|

---

## 5. 本地/系统依赖

| 名称 | 路径/来源 | 引入方式 | 说明 |
|------|----------|---------|------|

---

## 6. 仓库内置二进制（直接提交的 .so/.a）

⚠️ 这些文件直接存在于代码仓，架构绑定最严重

| 文件路径 | `file` 输出架构 | 用途 | 溯源结果 |
|----------|---------------|------|---------|

---

## 7. 构建工具二进制（脚本下载）

| 工具 | 下载地址 | 说明 |
|------|----------|------|

---

## 8. 系统包依赖（yum/apt）

| 包名 | 用途 | 安装命令 |
|------|------|---------|

---

## 9. 架构移植分析

### 9.1 🔴 预编译二进制（无源码，架构强绑定，必须替换）

> 这类依赖仅提供已编译好的 `.so`/`.a`/可执行文件，无法在新架构上直接使用。
> 已按第七步 7b 检测目标架构，并按 7c 的四级溯源优先级给出处理建议。

| 依赖名称 | 二进制内容 | 探测架构 | 引入方式 | 溯源级别 | 处理建议 |
|----------|-----------|---------|---------|---------|---------|
| `<依赖名>` | `lib/lib*.so`、`lib/lib*.a` | x86-64 | `http_archive` / 脚本下载 | 第N级 | 见下方溯源说明 |

**溯源说明**（按第七步 7c 四级优先级填写）：

| 依赖名称 | 溯源级别 | 溯源结论 |
|----------|---------|---------|
| `<依赖名>` | 第1级：构建配置已有源码地址 | 可从 `<url>` 重新编译 |
| `<依赖名>` | 第2级：项目目录内有源码 | 路径：`<项目内路径>`，可直接用于 ARM 编译 |
| `<依赖名>` | 第3级：工作区内有源码 | 路径：`<工作区路径>`，可复用 |
| `<依赖名>` | 第4级：公开来源 | ✅ 系统包/鲲鹏软件仓有 ARM 版本 / 🟡 需自行从源码编译 / 🔴 需联系维护团队 |

### 9.2 🟢 开源可编译源码（含完整源码，可直接交叉编译）

> 这类依赖以源码包（`.zip`/`.tar.gz`）形式下载或通过公开 Git 拉取，
> 包含完整 C/C++ 源码，可在 ARM 架构上正常编译。只需验证编译选项兼容性。

| 依赖名称 | 版本 | 说明 |
|----------|------|------|
| `<依赖名>` | `<版本>` | 纯 C/C++ 源码，平台无关 |

### 9.3 🟡 内部私仓源码（内网 Git，有源码但需确认 ARM 兼容性）

> 这类依赖通过内网 Git 拉取，理论上有源码可重新编译，但需要确认代码本身
> 是否存在 x86 架构特定代码（如内联汇编、`__x86_64__` 宏等）。

| 依赖名称 | 仓库地址 | 当前版本 | ARM 分支 | ARM 提交 | 说明 & 处理建议 |
|----------|---------|---------|---------|---------|----------------|
| `<依赖名>` | `ssh://git@git.example.internal/...` | tag/commit | ✅/❌ | ✅/❌ | 确认是否需要切换到 ARM 分支 |

---

## ⚠️ 待用户手动确认清单

> 🚀 **移植提速建议：优先推动内部子仓库完成 ARM 适配，再编译主仓库**
>
> 当项目存在内部私仓依赖（`git_repository` / `new_git_repository` / submodule 中的内网地址）时，
> **应优先联系各子仓库维护团队完成 ARM 适配并发布稳定版本（tag / commit / 预编译包）**，
> 主仓库只需更新构建配置中对应的版本号即可直接构建，无需在主仓库侧做任何代码改动。
> 反之，若等主仓库编译报错再逐一反查，排查成本显著更高。

以下依赖为**非开源地址**且**未发现任何 ARM 兼容性证据**，请逐条确认：

- [ ] `<依赖名>` (`<url>`) — 未发现 ARM 分支或相关提交历史，请确认该库是否兼容 ARM/aarch64
- [ ] `<依赖名>` (`<url>`) — 预编译二进制，架构未知，请提供 ARM 版本或源码
