# migration-target-execution 调用提示词

用于根据 `migration-plan.json` 在鲲鹏 ARM64 目标环境执行系统迁移。

```text
请使用 migration-target-execution 执行系统迁移。
migration-plan.json 路径：【路径】
请按 Skill 流程完成目标预检、变更确认、计划内组件迁移和最终验收汇总。
```
